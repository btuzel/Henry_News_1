<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="BottomNavigationViewTextStyle">
        ...
        <item name="android:fontFamily">@font/semibold</item>
        ...
    </style>
    <style name="bottomstyle2">
        <item name="android:fontFamily">@font/extralight</item>
    </style>
    <style name="HiddenTitleTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@color/darkgradend</item>
        <item name="android:colorBackground">@color/darkgradend</item>
    </style>



</resources>
